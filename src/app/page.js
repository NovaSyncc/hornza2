import HomePageClient from './HomePageClient';

export const metadata = {
  title: 'Hornza - Rent, Buy & Book Verified Properties in East Africa',
  description: 'East Africa\'s trusted property marketplace. Rent apartments, buy homes, or book furnished stays — all video-verified. No broker fees, direct owner contact. Properties in Nairobi, Mombasa, and across East Africa.',
};

export default function HomePage() {
  return <HomePageClient />;
}
