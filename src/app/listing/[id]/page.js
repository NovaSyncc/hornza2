import ListingDetailClient from './ListingDetailClient';

export const metadata = {
  title: 'Property Details | Hornza',
  description: 'View verified property details, video walkthrough, pricing, and contact the owner directly on WhatsApp.',
};

export default function ListingDetailPage() {
  return <ListingDetailClient />;
}